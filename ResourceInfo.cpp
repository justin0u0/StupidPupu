#include "ResourceInfo.hpp"

ResourceInfo::ResourceInfo(std::string type, std::string img, int maximum_hp, int universality)
	: type(type), img(img), maximum_hp(maximum_hp), universality(universality) {
}

